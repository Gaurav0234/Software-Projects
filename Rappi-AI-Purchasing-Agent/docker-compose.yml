services:
  purchasing-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/purchasing.db
    volumes:
      - ./data:/app/data
